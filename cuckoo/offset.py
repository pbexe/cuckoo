from time import *
offset = timezone  # Prints the offset of the current time (eg -3)
time = strftime("%H:%M:%S") # Prints the current time.
timezone = strftime("%Z") # Prints the name of the timezone

